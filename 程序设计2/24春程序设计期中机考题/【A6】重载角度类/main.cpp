// 此文件为锁定文件，请勿填写此文件

#include "Angle.hpp"
using namespace std;

int main()
{
    Angle A, B;
    cin >> A >> B;
    cout << (A > B) << endl;
    cout << (A == B) << endl;
    cout << (A < B) << endl;
    cout << (A + B) << endl;
    cout << (A - B) << endl;
    return 0;
}